Create a function called `hasAvailableSeats` that takes the `passengers` array as an argument and returns `true` or `false` depending on if the array indicates that the car is full already or if the car has extra seats.

**Note:** We can assume that this car only has seats for a maximum of four passengers.

